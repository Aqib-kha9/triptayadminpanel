# Admin Panel Environment Variables
# Copy this to .env and update values as needed

# Backend API URL (required in production)
# In dev, Vite proxy forwards /api to localhost:5000, so leave empty or set to localhost
# In production (Vercel), set this to your Render backend URL
VITE_API_URL=https://triptaybackend.onrender.com